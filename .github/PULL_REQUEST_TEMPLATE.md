## Summary

<!-- What does this change and why? Link the issue it resolves, e.g. "Closes #12". -->

## Checklist

- [ ] `pnpm format:check`, `pnpm check`, `pnpm test` and `pnpm build` pass locally
- [ ] Documentation updated where behaviour changed (`README.md`, `docs/`, `CONTEXT.md`)
- [ ] New content fields are added to `src/content-model/collections.ts`, not to the generated adapters
- [ ] Demo content stays fictional (no real people, addresses or contact details)
