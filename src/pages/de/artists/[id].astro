---
import ArtistView from "@/views/ArtistView.astro";
import { artistPaths } from "@/routes";

export const getStaticPaths = artistPaths;

const { artist } = Astro.props;
---

<ArtistView locale="de" artist={artist} />
